= Float will remove the element from the normal flow (same as absolute).
= Text and inline elements will wrap around the floated element(doesn't same as absolute).
= The container will not adjust its height to the floated element.